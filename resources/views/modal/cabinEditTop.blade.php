
<a href="javascript:;" class="btn btn-sm btn-outline-primary px-4 cabinEdit" data-toggle="modal" 
    data-target="#cabinEdit"
    data-id="{{ $room->id }}"
    data-name="{{ $room->name }}"
    data-room_no="{{ $room->room_no }}"
    data-rent="{{ $room->rent }}"
>View</a>
